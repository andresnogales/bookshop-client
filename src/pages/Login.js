import { Fragment } from "react";
import LoginForm from "../components/Login/LoginForm";

const Login = (props) => {
  return (
    <Fragment>
      <h1>Login</h1>
      <LoginForm></LoginForm>
    </Fragment>
  );
};
export default Login;
